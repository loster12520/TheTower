# TheTower 0.0.2 测试文档模板

导语：本文档是 0.0.2 测试执行模板，覆盖后端接口、前端能力、黑盒回归与规范门禁验证；本模板先提供完整结构，测试结果在执行阶段逐项填写。

## 目录
- [1. 测试范围与环境](#1-测试范围与环境)
- [2. 后端接口测试（自动化）](#2-后端接口测试自动化)
- [3. 前端测试（按计划执行）](#3-前端测试按计划执行)
- [4. 黑盒功能测试](#4-黑盒功能测试)
- [5. 规范与门禁测试](#5-规范与门禁测试)
- [6. 缺陷记录与修复回归](#6-缺陷记录与修复回归)
- [7. 结论与发布建议](#7-结论与发布建议)

---

## 1. 测试范围与环境

### 1.1 测试范围
- 后端：统一响应体、错误码、日志链路、缓存、SQLite + Jimmer、迁移机制。
- 前端：运行时配置中心、统一请求层、统一 WebSocket 层、主题切换、样式抽离、`@` 别名导入。
- 端到端：模板创建、保存、运行、取消、删除主链路。

### 1.2 测试环境（待填写）
| 项目 | 内容 |
|---|---|
| 测试日期 | 待填写 |
| 操作系统 | Windows |
| 后端版本 | 待填写 |
| 前端版本 | 待填写 |
| 数据库版本 | 待填写 |
| 浏览器版本 | 待填写 |

### 1.3 执行入口（待填写）
- 后端启动命令：待填写
- 前端启动命令：待填写
- 后端自动化脚本：`test/0.0.2/backend-test.py`
- WS 自动化脚本：`test/0.0.2/ws-event-test.ps1`
- 测试结果文件：`report/0.0.2/backend-api-test-output.json`

---

## 2. 后端接口测试（自动化）

### 2.1 覆盖目标
- 统一响应结构 `requestId/data/error` 全覆盖。
- 错误码与 HTTP 映射一致。
- 缓存命中与失效行为可观测。
- SQLite 持久化 CRUD 与迁移流程可执行。

### 2.2 测试用例表（结果待执行）

| 用例ID | 测试项 | 接口/路径 | 前置条件 | 步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|---|
| BE-001 | 健康检查 | `GET /api/v1/health` | 服务已启动 | 调用接口 | 返回统一响应且状态正常 | 待执行 | 待执行 |
| BE-002 | 模板创建 | `POST /api/v1/templates` | 无 | 提交合法模板 | 返回创建结果，`error=null` | 待执行 | 待执行 |
| BE-003 | 模板参数校验 | `POST /api/v1/templates` | 无 | 提交非法参数 | 返回 `TT-0400-001/002` | 待执行 | 待执行 |
| BE-004 | 模板详情不存在 | `GET /api/v1/templates/{id}` | id 不存在 | 调用接口 | 返回 `TT-0404-001` | 待执行 | 待执行 |
| BE-005 | 运行创建 | `POST /api/v1/runs` | 模板存在 | 发起运行 | 返回 runId 且响应结构正确 | 待执行 | 待执行 |
| BE-006 | 运行取消非法状态 | `POST /api/v1/runs/{id}/cancel` | run 已结束 | 调用取消 | 返回 `TT-0422-001` | 待执行 | 待执行 |
| BE-007 | 错误码唯一性抽查 | 全量 | 错误码表已定义 | 抽查实现 | 无同义多码/一码多义 | 待执行 | 待执行 |
| BE-008 | 缓存命中验证 | `GET /api/v1/templates` | 缓存开启 | 连续两次读取 | 第二次命中缓存并可追踪 | 待执行 | 待执行 |
| BE-009 | 缓存主动失效 | `PUT /api/v1/templates/{id}` + GET | 缓存开启 | 写后读取 | 返回新数据且失效日志可见 | 待执行 | 待执行 |
| BE-010 | 持久化 CRUD | templates/runs | SQLite 可用 | 执行增删改查 | 与接口语义一致 | 待执行 | 待执行 |
| BE-011 | 数据库迁移初始化 | 启动流程 | 空库 | 启动服务 | 自动初始化成功 | 待执行 | 待执行 |
| BE-012 | 数据库迁移升级 | 启动流程 | 低版本库 | 启动服务 | 升级成功且数据可读 | 待执行 | 待执行 |

---

## 3. 前端测试（按计划执行）

### 3.1 覆盖目标
- 配置中心按环境生效。
- 请求层统一入口生效并可处理错误码。
- WS 工厂具备单例、重连、心跳、消息适配能力。
- 样式抽离与主题切换可见。

### 3.2 测试用例表（结果待执行）

| 用例ID | 测试项 | 页面/模块 | 前置条件 | 步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|---|
| FE-001 | 环境配置切换 | 配置中心 | 准备 dev/test 配置 | 切换配置启动 | API/WS 地址生效 | 待执行 | 待执行 |
| FE-002 | 请求层统一入口 | `services` | 页面已接入 | 触发多个接口请求 | 走统一拦截与错误处理 | 待执行 | 待执行 |
| FE-003 | token/header 注入 | 请求层 | 开启 auth | 发起请求 | 自动注入 header | 待执行 | 待执行 |
| FE-004 | 错误码提示映射 | 页面交互 | 构造后端错误码 | 触发错误 | 出现对应用户提示 | 待执行 | 待执行 |
| FE-005 | WS 单例复用 | 运行面板 | 同 key 多次连接 | 建连两次 | 返回同一连接实例 | 待执行 | 待执行 |
| FE-006 | WS 自动重连 | 运行面板 | 模拟断线 | 等待重连 | 指数退避重连成功 | 待执行 | 待执行 |
| FE-007 | WS 心跳保活 | 运行面板 | 长连接运行中 | 观察心跳 | 连接保持稳定 | 待执行 | 待执行 |
| FE-008 | 主题切换 | 全局 UI | 明暗主题可用 | 切换主题 | 组件样式继承生效 | 待执行 | 待执行 |
| FE-009 | 样式抽离检查 | 页面/组件 | 已迁移 | 代码检查 | 无新增大段内联样式 | 待执行 | 待执行 |
| FE-010 | `@` 别名导入检查 | `src` | 已改造 | 静态检查 | 无深层相对路径 | 待执行 | 待执行 |

---

## 4. 黑盒功能测试

### 4.1 主链路场景
1. 创建模板。
2. 编辑并保存步骤。
3. 发起运行并接收 WS 事件。
4. 取消运行或等待完成。
5. 删除模板。

### 4.2 黑盒用例表（结果待执行）

| 用例ID | 场景 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| E2E-001 | 模板全链路 | 创建→编辑→保存 | 模板数据持久化成功 | 待执行 | 待执行 |
| E2E-002 | 运行监控链路 | 运行→WS 事件→终态 | 节点状态与日志正确更新 | 待执行 | 待执行 |
| E2E-003 | 取消运行链路 | 运行中点击取消 | 终态为 CANCELED | 待执行 | 待执行 |
| E2E-004 | 删除链路 | 删除模板后查询 | 返回不存在提示 | 待执行 | 待执行 |

---

## 5. 规范与门禁测试

### 5.1 检查项
- 注释白名单合规率 >= 95%。
- 函数复杂度超限项清零或有豁免。
- 文件长度超限项清零或有拆分计划。
- 命名词典一致率 >= 95%。
- `@` 别名导入使用率可验证。

### 5.2 结果表（待执行）

| 检查项 | 工具/方式 | 阈值 | 结果 | 结论 |
|---|---|---|---|---|
| 注释白名单合规率 | 抽查 + 脚本统计 | >=95% | 待执行 | 待执行 |
| 复杂度阈值 | lint/静态检查 | <=10 | 待执行 | 待执行 |
| 文件长度阈值 | lint/脚本 | <=400 行 | 待执行 | 待执行 |
| 命名词典一致率 | 代码评审清单 | >=95% | 待执行 | 待执行 |
| 别名导入使用率 | 静态扫描 | 100%（src） | 待执行 | 待执行 |

---

## 6. 缺陷记录与修复回归

### 6.1 缺陷记录表（待执行）

| 缺陷ID | 发现阶段 | 严重级别 | 描述 | 根因 | 修复方案 | 回归结果 |
|---|---|---|---|---|---|---|
| BUG-001 | 待执行 | 待定 | 待填写 | 待填写 | 待填写 | 待填写 |

### 6.2 修复回归要求
- 每个高优先级缺陷必须附回归记录。
- 回归需覆盖“原问题 + 相邻风险路径”。

---

## 7. 结论与发布建议

### 7.1 测试结论（待填写）
- 后端结论：待填写
- 前端结论：待填写
- 黑盒结论：待填写

### 7.2 发布建议（待填写）
- 是否允许发布：待填写
- 阻塞项：待填写
- 遗留风险：待填写

总结：本模板确保 0.0.2 测试执行可追踪、可复现、可量化，后续按实际执行结果逐项填充即可形成正式测试报告。
