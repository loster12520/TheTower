# TheTower 毕业论文 PPT 大纲

## 基本信息
- **时长**：5 分钟（PPT 讲述）+ 3 分钟（现场演示）
- **风格**：瑞士国际主义（无衬线 + 网格点阵 + IKB/柠檬黄/安全橙高亮）
- **设计原则**：大字、少字、多图、多数据
- **侧重**：技术创新 : 工程完整度 ≈ 2:1

---

## 页面结构（共 14 页）

### P1 — 封面
- **内容**：论文题目、学生姓名、学院、指导教师、日期
- **设计**：瑞士风格大标题，单色背景 + 网格点阵，字号 48pt+
- **备注**：10 秒，第一印象

### P2 — 问题与动机（一页定调）
- **内容**：
  - 一句话痛点：浏览器重复操作 → 人工低效易错
  - 三列对比：纯脚本方案（高门槛）vs 商业 RPA（高成本）vs TheTower（低代码+低成本）
- **视觉**：三列对比卡片，用颜色区分优劣
- **备注**：30 秒，让评委立刻理解"为什么做"

### P3 — 核心创新：画布 + 浏览器自动化
- **内容**：
  - 核心公式：ReactFlow 可视化编排 × Playwright 浏览器执行 = 低代码 RPA
  - 一句话：拖拽节点 → 配置参数 → 连线编排 → 驱动真实浏览器
- **视觉**：简化版"画布→执行→回传"三阶段流程图（大图占主体）
- **备注**：40 秒，**核心论点页**，整场答辩的锚点

### P4 — 系统架构总览（简化版）
- **内容**：
  - 五层架构：前端展示层 → 通信协议层 → 后端服务层 → 执行引擎层 → 数据存储层
  - 标注关键组件：ReactFlow / REST+WebSocket / Ktor / Playwright / SQLite
- **视觉**：纵向分层图，每层 1-2 个关键词，去除论文原文的 Mermaid 细节
- **备注**：25 秒，建立系统全貌认知

### P5 — 技术选型（极简一览）
- **内容**：
  - ReactFlow → 节点式画布引擎
  - Playwright → 跨浏览器自动化内核
  - Ktor + WebSocket → 轻量异步服务 + 实时事件流
  - SQLite → 零配置本地持久化
- **视觉**：四列图标卡片，每个一行说明
- **备注**：15 秒快速带过

### P6 — 亮点 1：可视化编排闭环（重点页）
- **内容**：
  - 三栏编辑器：步骤库 → 画布拖拽连线 → 配置面板
  - 体验增强：自动保存、自动布局、撤销重做、搜索/标签/克隆
- **视觉**：编辑器界面截图（大图铺满）+ 关键交互标注
- **备注**：30 秒，展示"低代码"的直观性

### P7 — 亮点 2：运行监控实时闭环（重点页）
- **内容**：
  - REST 管理 + WebSocket 事件流双轨协议
  - 8 种运行事件：RUN_STARTED → STEP_STARTED → STEP_SUCCEEDED / FAILED → LOG → RUN_SUCCEEDED
- **视觉**：运行时截图（状态面板 + 日志 + 事件时间线）+ 事件流示意
- **备注**：30 秒，展示"可观测性"

### P8 — 亮点 3：调试工作台（重点页）
- **内容**：
  - 条件断点 → 暂停执行 → 上下文查看
  - 时间旅行 → 回看历史快照
  - 远程操控 → 调试预览中直接操作浏览器
- **视觉**：调试界面截图 + 三步箭头图
- **备注**：30 秒，展示"不只是能跑，更能排查"

### P9 — 数据与集成能力
- **内容**：
  - 节点类型矩阵：页面操作 | 等待 | 数据提取 | 文件/Excel | 剪贴板 | 网络监听
  - 一句话：从页面操作到数据落盘，覆盖完整业务链路
- **视觉**：节点类型图标网格（不用列表，用图标卡片）
- **备注**：20 秒走马观花，为演示做铺垫

### P10 — 产品化能力闭环
- **内容**：
  - 模板市场（发布/导入）+ 调度任务（单次/周期）+ 认证与工作空间 + 协作编辑
  - 六个模块的打钩表：编排✓ 执行✓ 调试✓ 数据✓ 平台✓ 协议✓
- **视觉**：四列功能卡片 + 下方"首版闭环"总览表
- **备注**：20 秒，证明不是原型而是可交付系统

### P11 — 测试数据总览
- **内容**：
  - 142 项测试用例，通过率 97.2%
  - 7 大模块数据（大数字 + 进度条）
  - 性能指标：50 节点画布流畅、3 浏览器兼容、3 流程并发
- **视觉**：仪表盘风格，大数字 + 进度条 + 徽章
- **备注**：20 秒，用数据证明工程严谨性

### P12 — 核心设计决策（简略）
- **内容**：
  - 为什么 ReactFlow 而非自建画布？→ 成熟节点模型 + 生态
  - 为什么 REST + WebSocket 而非纯轮询？→ 管理清晰 + 实时低延迟
  - 为什么 SQLite 而非 PostgreSQL？→ 毕业设计场景零运维
- **视觉**：三行 Q&A 卡片
- **备注**：15 秒，点到为止

### P13 — 总结与展望
- **内容**：
  - 已完成：可视化编排 + 浏览器执行 + 调试排障 + 产品化闭环
  - 待改进：协作冲突自动合并 / 细粒度权限 / 长流程自动化验证 / 更强持久化
- **视觉**：左侧成果清单（绿色标记），右侧展望（灰色标记），比例约 3:1
- **备注**：20 秒，成果收尾为主，不足一笔带过

### P14 — 致谢
- **内容**：感谢指导教师、同学朋友、学院
- **设计**：简洁收尾，留二维码或联系方式
- **备注**：5 秒，过渡到演示环节

---

## 时间分配总览

| 段落 | 页码 | 时长 |
|------|------|------|
| 开场（封面 + 问题 + 核心创新） | P1-P3 | 1'20" |
| 架构与技术（架构 + 技术选型） | P4-P5 | 40" |
| 三大亮点（编排 + 监控 + 调试） | P6-P8 | 1'30" |
| 能力展示（数据 + 产品化 + 测试） | P9-P11 | 1'00" |
| 收尾（决策 + 总结 + 致谢） | P12-P14 | 40" |
| **合计** | **14 页** | **5'10"** |

---

## 待准备素材清单

- [ ] 编辑器三栏布局截图（步骤库 + 画布 + 配置面板）
- [ ] 运行监控面板截图（状态 + 日志 + 事件流）
- [ ] 调试工作台截图（断点 + 上下文 + 预览）
- [ ] 模板首页截图（搜索 + 标签 + 卡片列表）
- [ ] 模板市场截图
- [ ] 系统架构简化图（基于论文第4章，去除技术细节）
- [ ] 执行链路简化流程图（画布→REST→执行器→浏览器→WS→监控）
- [ ] 测试数据仪表盘（142 用例 / 97.2% / 七大模块数据）
