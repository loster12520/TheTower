# TheTower 毕业论文 PPT 细纲

## 一、全局设计规范

### 1.1 视觉系统（瑞士国际主义风格）

| 要素 | 规范 |
|------|------|
| **主字体** | Helvetica / Inter / Noto Sans SC，标题 42-56pt，正文 20-24pt，标注 14-16pt |
| **背景** | 纯白底 `#FFFFFF` 或极浅灰 `#F5F5F5`，大面积留白 |
| **主色** | IKB 蓝 `#002FA7` — 用于标题、关键数字、重点标注 |
| **辅色** | 柠檬黄 `#FFE600` — 用于高亮标签、强调区域背景 |
| **警示色** | 安全橙 `#FF5A00` — 用于痛点标注、数据警示 |
| **文字色** | 正文黑 `#1A1A1A`，次级灰 `#666666` |
| **装饰** | 网格点阵（dot grid，2px 间距 20px）作为局部背景纹理 |
| **分割** | 细线 `1px #DDDDDD` 或留白间距，避免粗边框 |
| **图表** | 柱状图/进度条用 IKB 蓝填充，对比项用 20% 透明度蓝区分 |

### 1.2 每页统一结构

```
┌──────────────────────────────────────────────┐
│  章节标签（14pt, 柠檬黄底色胶囊标签, 左上角）  │
│                                              │
│  主标题（48pt, IKB蓝, 左对齐, 占1/3高度）      │
│                                              │
│  视觉主体区（占 55-65% 面积）                   │
│  - 截图 / 架构图 / 数据仪表盘 / 流程图          │
│  - 文字极少，用标注箭头辅助理解                  │
│                                              │
│  底部备注（14pt, 灰色, 1-2行可选）              │
└──────────────────────────────────────────────┘
```

### 1.3 动画策略

- 页面切换：简单淡入淡出（0.3s），不用花哨转场
- 内容呈现：单页内不做逐条弹出，整页一次呈现
- 标注箭头：静态 SVG，不用动画
- **原则：快进快出，不给动画留时间**

---

## 二、逐页细纲

---

### P1 — 封面

**定位**：10 秒 / 第一印象 / 不讲解直接翻

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│  [网格点阵纹理覆盖右上 1/3 区域]               │
│                                              │
│                                              │
│   基于 Playwright 的                           │
│   浏览器 RPA 画布                              │
│   工作流系统                                   │
│   ─────────────────────                      │
│   TheTower                                   │
│                                              │
│          [页面底部]                            │
│          东莞理工学院 · 计算机科学与技术学院       │
│          学生：XXX  ·  指导教师：XXX 副教授       │
│          2026 年 5 月                         │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 主标题 48pt IKB 蓝，三行中文，「TheTower」56pt 加粗
- 右上角 1/3 区域叠加 2px 间距网格点阵纹理（IKB 蓝 15% 透明度）
- 底部信息 16pt 灰色，两行
- 无章节标签

**讲解**：不讲解。站定后直接翻页。

**转场**：→ P2，一句话 "各位老师好，我做的课题是浏览器 RPA 画布工作流系统。"

---

### P2 — 问题与动机

**定位**：30 秒 / 快速建立共识 / 「为什么做这个课题」

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   PROBLEM                                      │
│                                              │
│   浏览器重复操作的困境                           │
│                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  │  纯脚本方案    │  │  商业 RPA     │  │  TheTower    │
│  │              │  │              │  │              │
│  │  Selenium    │  │  UiPath      │  │  画布编排     │
│  │  Playwright  │  │  来也        │  │  +           │
│  │              │  │              │  │  浏览器执行   │
│  │  ✗ 需编程基础 │  │  ✗ 授权昂贵   │  │              │
│  │  ✗ 维护成本高 │  │  ✗ 配置复杂   │  │  ✓ 低代码     │
│  │  ✗ 无可视化   │  │  ✗ 定制受限   │  │  ✓ 低成本     │
│  │              │  │              │  │  ✓ 可观测     │
│  └──────────────┘  └──────────────┘  └──────────────┘
│     [浅灰背景]        [浅灰背景]        [IKB蓝边框]    │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`PROBLEM`，14pt，柠檬黄胶囊
- 主标题 "浏览器重复操作的困境"，42pt
- 三列卡片等高排列，前两列浅灰底 + 红色叉号，第三列 IKB 蓝 2px 边框 + 白底 + 绿色对勾
- 每列 3 个要点，20pt 正文
- TheTower 列比两侧略宽（视觉中心略偏右）

**讲稿**：
> "企业里大量重复性浏览器操作——表单录入、信息采集、状态监控——目前要么靠写脚本，要么买商业 RPA。写脚本门槛高，业务人员用不了；商业 RPA 授权贵、配置重，小团队买不起也改不动。所以我的课题方向很明确：能不能把浏览器自动化的门槛降下来，同时保留足够的执行能力？"

**转场**：→ P3，"这就引出我的核心思路——"

---

### P3 — 核心创新

**定位**：40 秒 / **整场答辩的锚点** / 一句话立论

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   INNOVATION                                   │
│                                              │
│         画布  ×  浏览器  =  低代码 RPA           │
│                                              │
│   ┌─────────┐     ┌──────────┐     ┌─────────┐ │
│   │  🖱️     │     │  🌐      │     │  📊     │ │
│   │ 拖拽节点 │ ──→ │ 驱动真实  │ ──→ │ 实时回传 │ │
│   │ 配置参数 │     │ 浏览器执行 │     │ 运行状态 │ │
│   │ 连线编排 │     │          │     │ 调试信息 │ │
│   └─────────┘     └──────────┘     └─────────┘ │
│    前端 ReactFlow   Playwright       WebSocket  │
│                                              │
│          非程序员也能搭建自动化流程               │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`INNOVATION`，柠檬黄胶囊
- 主标题公式 "画布 × 浏览器 = 低代码 RPA"，48pt IKB 蓝，乘号和等号用安全橙
- 三个圆形/圆角方块横向排列，箭头连接（粗 3px IKB 蓝箭头）
- 每个方块内：emoji 图标（可选）/ 简洁中文 24pt + 下方灰色 14pt 技术标注
- 底部结论句 18pt 灰色

**讲稿**：
> "核心思路一句话概括：ReactFlow 可视化画布 × Playwright 浏览器执行等于低代码 RPA。左边，用户在画布上拖拖拽拽、连连线就把流程搭好了；中间，后端拿到流程定义，用 Playwright 驱动真实浏览器一步步执行；右边，运行过程中状态、日志、错误实时通过 WebSocket 推回前端。用户不需要写代码，只要理解自己的业务流程就行。"

**转场**：→ P4，"这个思路落地后，系统的整体结构是这样的——"

---

### P4 — 系统架构总览

**定位**：25 秒 / 快速建立全貌 / 不展开细节

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   ARCHITECTURE                                 │
│                                              │
│   ┌──────────────────────────┐               │
│   │   前端展示层               │  React        │
│   │   画布 · 配置 · 监控 · 调试 │  ReactFlow    │
│   ├──────────────────────────┤               │
│   │   通信协议层               │  REST + WS    │
│   ├──────────────────────────┤               │
│   │   后端服务层               │  Ktor         │
│   │   模板 · 运行 · 调试       │  Kotlin       │
│   │   调度 · 认证 · 协作       │               │
│   ├──────────────────────────┤               │
│   │   执行引擎层               │  Playwright   │
│   │   步骤解析 · 浏览器驱动     │  Java         │
│   ├──────────────────────────┤               │
│   │   数据存储层               │  SQLite       │
│   │   模板 · 运行 · 产物       │  文件存储      │
│   └──────────────────────────┘               │
│                                              │
│   左侧：界面与交互  ·  右侧：逻辑与数据           │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`ARCHITECTURE`
- 五层堆叠矩形，每层高度一致，左对齐
- 每层左侧大标签（20pt IKB 蓝粗体），右侧灰字列关键组件（14pt）
- 中间一条竖虚线分隔"前端侧"和"后端侧"
- 不出现 Mermaid 原图的复杂箭头，只保留层叠关系

**讲稿**：
> "系统采用标准前后端分离。从上到下五层：最上是 React + ReactFlow 的画布界面；通信层用 REST 做管理操作、WebSocket 做实时推送；服务层是 Ktor 写的后端，负责模板、运行、调试、调度这些业务逻辑；执行层用 Playwright 驱动真实浏览器；底层 SQLite 做持久化。核心就一句话——界面管编排，后端管执行，两边通过双通道协议解耦。"

**转场**：→ P5，"具体技术选型我一页带过——"

---

### P5 — 技术选型一览

**定位**：15 秒 / 极速带过 / 不给评委提问时间

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   TECH STACK                                   │
│                                              │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│   │          │ │          │ │          │ │          │
│   │ ReactFlow│ │Playwright│ │  Ktor    │ │ SQLite   │
│   │  画布引擎 │ │ 自动化内核│ │ 异步服务  │ │ 本地存储  │
│   │          │ │          │ │          │ │          │
│   │ v12.10   │ │ v1.6.3   │ │ v2.3.12  │ │ v3.46    │
│   └──────────┘ └──────────┘ └──────────┘ └──────────┘
│    ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│    │  React   │ │   Umi    │ │ Ant Design│ │  MobX    │
│    │  v19.2   │ │  v4.6    │ │  v6.2     │ │  v6.15   │
│    └──────────┘ └──────────┘ └──────────┘ └──────────┘
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`TECH STACK`
- 8 个技术卡片，2 行 × 4 列，间距均匀
- 每个卡片：灰边框 1px，圆角 8px，上方技术名 22pt IKB 蓝，下方中文说明 14pt 灰，版本号 12pt 更浅灰
- 第一行为核心技术栈，第二行为辅助框架
- 纯网格对齐，零文字段落

**讲稿**：
> "技术选型。前端 React + ReactFlow 搭画布，Umi 做工程化；后端 Ktor + Playwright 做执行；SQLite 做存储。都是成熟方案，不展开。"

**转场**：→ P6 无缝衔接 "下面进入三个核心亮点——首先是可视化编排。"

**答辩预埋**：如果评委追问"为什么选 ReactFlow 而非自建"——回答话术见 P12 详细展开。

---

### P6 — 亮点 1：可视化编排闭环 ★

**定位**：30 秒 / 第一重点 / 展示「低代码」直观性

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   HIGHLIGHT ①                                  │
│                                              │
│   拖拽即编排，无需写一行代码                      │
│                                              │
│  ┌────────────────────────────────────────────┐
│  │                                            │
│  │         [编辑器三栏布局截图]                  │
│  │                                            │
│  │   左侧       中间         右侧               │
│  │   步骤库     画布区       配置面板             │
│  │   (节点列表)  (ReactFlow   (参数表单)          │
│  │             拖拽+连线)                       │
│  │                                            │
│  └────────────────────────────────────────────┘
│                                              │
│   🏷 自动保存  ·  🏷 自动布局  ·  🏷 撤销重做     │
│   🏷 搜索分组  ·  🏷 克隆复制  ·  🏷 批量删除     │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`HIGHLIGHT ①`
- 标题 "拖拽即编排，无需写一行代码"，42pt
- **核心视觉**：编辑器整页截图占 60% 面积，此图应清晰展示三栏结构
- 截图区域用数字标注①②③对应下方三栏说明（按瑞士风格，用极细的 IKB 蓝引线 + 12pt 数字圆圈）
- 底部一行标签胶囊（柠檬黄底色，14pt），列出 6 个体验增强项
- 截图尺寸建议：1440×900 以上，确保投影清晰

**讲稿**：
> "第一是可视化编排。打开编辑器，左边步骤库列了所有可用节点——打开网页、点击、输入、等待、提取数据、读写文件等等。从左边拖一个节点到中间画布，连上线，点开右侧面板配置参数——一个自动化流程就搭好了。编辑器还补齐了自动保存、自动布局、撤销重做、搜索克隆这些高频操作，不是说能拖就算完，得让人长期用着顺手。"

**截图要求**：
- 文件名建议：`screenshot-editor.png`
- 截图内容：编辑器页面，画布上有 5-8 个节点并已连线，右侧显示某个节点的参数配置面板
- 特别要求：左侧步骤库要能看清节点分类

**转场**：→ P7 "编排只是第一步，编完了怎么跑、跑的过程中发生了什么——这是第二个亮点。"

---

### P7 — 亮点 2：运行监控实时闭环 ★

**定位**：30 秒 / 第二重点 / 展示「可观测性」

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   HIGHLIGHT ②                                  │
│                                              │
│   每一秒都知道运行到了哪里                        │
│                                              │
│  ┌──────────────────────────────┐            │
│  │  [运行监控面板截图]             │            │
│  │  左侧：步骤列表 + 状态指示灯     │            │
│  │  右侧：实时日志输出             │            │
│  └──────────────────────────────┘            │
│                                              │
│   REST 管理 ──→  创建/取消/重启运行              │
│   WebSocket ──→  RUN_STARTED → STEP_STARTED   │
│                  → STEP_SUCCEEDED / FAILED    │
│                  → LOG → RUN_SUCCEEDED        │
│                                              │
│   8 种事件类型，毫秒级推送                       │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`HIGHLIGHT ②`
- 标题 "每一秒都知道运行到了哪里"，42pt
- 上方为运行监控截图（占 45% 面积）
- 截图下方左侧：REST 管理操作，3 个关键词，灰底小方块
- 截图下方右侧：事件流，用箭头串联 5 种关键事件类型，IKB 蓝渐变色块
- 底部署名 "8 种事件类型，毫秒级推送"，18pt

**讲稿**：
> "第二是运行监控。用户点运行后，后端创建执行会话、打开浏览器、按步骤顺序执行。这不是黑盒——每一步开始、成功、失败、日志输出，全部通过 WebSocket 实时推到前端。你能在面板上清楚看到：当前卡在第几步、已完成的步骤是绿的、失败的步骤标红、实时日志滚动输出。这个闭环把浏览器自动化从"跑完看结果"变成了"跑的过程中全程可观测"。"

**截图要求**：
- 文件名建议：`screenshot-run-monitor.png`
- 截图内容：运行监控面板，步骤列表中有成功（绿）和进行中（蓝）状态，右侧日志区有内容

**转场**：→ P8 "流程跑失败了怎么办？这就是第三个亮点——"

---

### P8 — 亮点 3：调试工作台 ★

**定位**：30 秒 / 第三重点 / 展示「可排查性」/ 竞争力核心

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   HIGHLIGHT ③                                  │
│                                              │
│   出了问题，能定位、能回溯、能远程操控              │
│                                              │
│           ┌──────────┐                       │
│   断点命中 → │ 条件断点  │                       │
│           │ 命中即停  │                       │
│           └──────────┘                       │
│                ↓                              │
│           ┌──────────┐                       │
│   时间旅行 → │ 变量快照  │                       │
│           │ 历史回溯  │                       │
│           └──────────┘                       │
│                ↓                              │
│           ┌──────────┐                       │
│   远程操控 → │ 预览区    │                       │
│           │ 直接操作  │                       │
│           └──────────┘                       │
│                                              │
│  [右下角：调试工作台截图小图，展示预览画面+断点面板]   │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`HIGHLIGHT ③`
- 标题 "出了问题，能定位、能回溯、能远程操控"，42pt
- 三个方块纵向排列，IKB 蓝左侧竖线连接形成流程感
- 每个方块：左侧大标签（24pt IKB 蓝粗），右侧功能说明（16pt 灰）
- 右下角配一张调试预览的小截图（占 25% 面积）
- 整页留白较多，信息密度低，强调"这是一个完整工作台"

**讲稿**：
> "第三是调试。自动化流程跑失败了，光告诉你'失败了'没有用——你得知道失败在哪一步、当时页面是什么状态、变量值是多少。TheTower 的调试模式做了三件事：条件断点——你可以设定满足某个条件时自动暂停；时间旅行——暂停后可以回看历史步骤的变量快照和页面状态；远程操控——在调试预览里直接点击、输入，操控那个真实的浏览器。对于复杂流程来说，这个能力决定了你花 5 分钟还是 2 小时排查问题。"

**截图要求**：
- 文件名建议：`screenshot-debug.png`
- 截图内容：调试工作台，显示断点暂停状态、左侧有变量上下文面板、右侧有浏览器预览画面

**转场**：→ P9 "以上是三大核心亮点。下面快速过一遍系统的其他能力——为现场演示做铺垫。"

---

### P9 — 数据与集成节点

**定位**：20 秒 / 走马观花 / 为演示铺垫节点丰富度

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   CAPABILITIES                                 │
│                                              │
│   不止点击输入，覆盖完整数据链路                   │
│                                              │
│   ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │
│   │ 页面操作│ │ 数据提取│ │文件/Excel│ │网络监听 │ │
│   │        │ │        │ │        │ │        │ │
│   │ openUrl│ │ extract│ │saveData│ │listen  │ │
│   │  click │ │  文本   │ │saveExcel│ │Request │ │
│   │  type  │ │  属性   │ │import  │ │Trigger │ │
│   │  wait  │ │        │ │ Excel  │ │ Result │ │
│   └────────┘ └────────┘ └────────┘ └────────┘ │
│                                              │
│   页面操作 → 数据提取 → 文件落盘 → 网络观测       │
│   一个流程跑通完整业务闭环                        │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`CAPABILITIES`
- 标题 "不止点击输入，覆盖完整数据链路"，42pt
- 四列卡片，每列一个类别名（20pt IKB 蓝）+ 2-3 个节点名（16pt 灰）
- 下方一条横向箭头串联四个块，标注"完整业务链路"
- 零截图，纯图标+文字——这页是过渡页，信息密度特低

**讲稿**：
> "节点方面，除了基础的打开网页、点击、输入、等待，系统还支持数据提取、文件读写、Excel 导入导出、剪贴板读取，甚至网络请求监听。这意味着一个自动化流程可以从导入 Excel 数据开始，逐个录入网页，截取响应结果，最后导出处理报告——完整的数据链路。"

**转场**：→ P10 "功能做全了不算完，还得做成一个能交付的系统——"

---

### P10 — 产品化能力闭环

**定位**：20 秒 / 证明「不是原型，是可交付系统」

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   PRODUCT                                      │
│                                              │
│   从工具到平台的六个模块                          │
│                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  │  模板市场  │ │  调度任务  │ │  认证     │ │  协作编辑  │
│  │           │ │          │ │  工作空间  │ │          │
│  │  发布/导入 │ │ 单次/周期 │ │  登录/隔离│ │ 分享/采纳 │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘
│                                              │
│   ┌─────────────────────────────────────────┐ │
│   │ 编排 ✓ │ 执行 ✓ │ 调试 ✓ │ 数据 ✓ │ 平台 ✓ │ 协议 ✓ │ │
│   └─────────────────────────────────────────┘ │
│              首版能力闭环已完成                  │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`PRODUCT`
- 四列功能卡片，每张浅灰底 + IKB 蓝标题
- 下方打钩表：6 个模块名 + 绿色对勾，用一行紧凑排列
- 最下部署名句，IKB 蓝

**讲稿**：
> "除了核心的编排执行调试，0.1.0 版本还补齐了产品化能力：模板市场支持发布和导入复用；调度任务支持单次和周期触发；认证与工作空间做多用户隔离；协作编辑支持模板分享和在线成员感知。六个模块全部闭环，系统不再只是一个技术原型。"

**转场**：→ P11 "接下来用数据说话——"

---

### P11 — 测试数据仪表盘

**定位**：20 秒 / 用数据证明工程严谨性 / 一面即过

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   QUALITY                                      │
│                                              │
│   测试数据                                      │
│                                              │
│         ┌──────────┐    ┌──────────┐          │
│         │   142    │    │  97.2%  │          │
│         │  测试用例  │    │  通过率   │          │
│         └──────────┘    └──────────┘          │
│                                              │
│   模板管理 ████████████████  22/22  100%      │
│   流程编排 ███████████████▌  20/21  95.2%     │
│   运行调试 ████████████████  48/48  100%      │
│   数据集成 █████████████▌    27/30  90.0%     │
│   平台能力 ████████████████  7/7    100%      │
│   协议存储 ████████████████  9/9    100%      │
│   性能兼容 ████████████████  5/5    100%      │
│                                              │
│   性能指标：50节点流畅 · 3浏览器兼容 · 3流程并发    │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`QUALITY`
- 顶部两个超大数字（72pt IKB 蓝，不加粗），下方 18pt 灰标注
- 中间 7 行进度条，左对齐模块名（16pt），进度条用 IKB 蓝填充，右侧 "通过/总数 百分比"
- 进度条设计：灰色底 bar + IKB 蓝填充部分，高度 8px，圆角
- 底部一行性能指标胶囊标签

**讲稿**：
> "测试方面。整个项目从 0.0.1 到 0.1.0 十个版本迭代推进，累计 142 项用例，总通过率 97.2%。七大模块中五个 100% 通过。性能上，50 节点画布流畅拖拽、Chrome/Firefox/Edge 三浏览器兼容、3 流程并发执行，这些指标都通过了验证。"

**转场**：→ P12 "最后我想简单提三个设计选择——"

---

### P12 — 核心设计决策 + 答辩预埋

**定位**：15 秒讲 PPT + 预留答辩应答储备

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   DECISIONS                                    │
│                                              │
│   三个关键设计选择                               │
│                                              │
│   Q  为什么 ReactFlow 而非自建画布？               │
│   A  内置节点/边模型 + 拖拽缩放 + 生态成熟          │
│                                              │
│   Q  为什么 REST + WebSocket 而非纯轮询？           │
│   A  管理操作清晰 + 步骤级事件毫秒级延迟             │
│                                              │
│   Q  为什么 SQLite 而非 PostgreSQL？               │
│   A  零配置本地部署 + 毕设规模完全够用               │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`DECISIONS`
- 三行 Q&A 卡片，每行左侧 Q 标签（安全橙 14pt 胶囊），右侧 A 内容（18pt 浅灰正文）
- 极简设计，三行均匀分布，留白为主
- 不配图

**讲稿（PPT 版本，15 秒）**：
> "三个关键技术决策简单提一下。选 ReactFlow 是因为它内置节点和边的数据结构，不需要从零实现画布层。选 REST + WebSocket 双通道是因为管理接口需要清晰，执行状态需要实时。选 SQLite 是因为毕业设计场景下零运维、零配置、单文件部署。"

---

**答辩预埋话术（不展示在 PPT 上）**：

> **如果评委追问"为什么不直接用 Selenium？"**
> "Playwright 相比 Selenium 有三个明显优势：自动等待机制让脚本更稳定，不需要手动写 waitFor；原生支持多浏览器（Chromium/Firefox/WebKit）同一套 API；网络拦截和请求监听能力更强，这对我们后面的 listenRequest 节点是关键依赖。简单说，Selenium 能做但更费劲，Playwright 做起来更干净。"

> **如果评委追问"和 UiPath / 来也这类商业 RPA 比，你的差异化在哪？"**
> "定位不同。商业 RPA 面向企业级交付，重在全套服务、技术支持和生态绑定，代价是授权昂贵且定制受限。TheTower 面向的是'需要一个轻量自动化工具'的场景——开源、本地部署、单文件数据库，想改代码可以改，想加节点可以加。这种灵活性是商业平台给不了的。"

> **如果评委追问"系统最难的技术点是什么？"**
> "执行引擎的设计。因为要在不暴露 Playwright 细节的前提下，把抽象节点稳定映射为具体的浏览器操作。每个节点的执行是异步的、可能失败、需要上下文传递、需要调试支持。这个映射层既要简单好扩展，又要保证执行可靠，花了最多精力。"

> **如果评委追问"节点的可扩展性怎么保证？"**
> "前端通过 stepRegistry 注册表统一管理，新增节点只需定义 type + config schema；后端执行器通过类型分发机制处理，新增节点只需增加一个执行分支。目前从 0.0.1 到 0.1.0，节点从五六种扩展到十余种，没有改过核心架构。"

**转场**：→ P13 "做个总结——"

---

### P13 — 总结与展望

**定位**：20 秒 / 成果收尾 / 不足一笔带过

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│   CONCLUSION                                   │
│                                              │
│   已完成                   待改进               │
│                                              │
│   ✓ 可视化流程编排           · 协作冲突自动合并    │
│   ✓ 浏览器自动化执行         · 细粒度权限控制     │
│   ✓ 调试排障工作台           · 长流程自动化验证    │
│   ✓ 首版产品化闭环           · 持久化能力升级     │
│                                              │
│   ┌─────────────────────────────────────────┐ │
│   │                                         │ │
│   │  从"让浏览器自动化更简单"出发              │ │
│   │  完成了一个可编排、可执行、可调试、可协作     │ │
│   │  的低代码浏览器 RPA 系统                   │ │
│   │                                         │ │
│   └─────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 章节标签：`CONCLUSION`
- 左侧（占 55%）：4 条已完成，绿色对勾 + 16pt 正文
- 右侧（占 35%）：4 条待改进，灰色圆点 + 14pt 浅灰正文
- 中间 IKB 蓝竖细线分割
- 底部引用框（柠檬黄底色），总结句 18pt
- 左右两侧视觉比重约 3:1

**讲稿**：
> "做个总结。TheTower 从'让浏览器自动化更简单'这个出发点，完成了一个具备可视化编排、浏览器执行、调试排障和产品化首版能力的工作流系统。不足的方面，协作的自动合并、更细的权限、更完整的自动化测试、更强的存储——这些是后续可以继续做的方向。以上就是我的汇报，接下来我花三分钟给大家做一个现场演示。"

**转场**：→ P14 "——"（致谢后直接进入演示）

---

### P14 — 致谢 + 过渡页

**定位**：5 秒 / 致谢 + 自然过渡到演示

**视觉布局**：
```
┌──────────────────────────────────────────────┐
│                                              │
│                                              │
│             感谢各位老师                        │
│             请多指教                            │
│                                              │
│             ────────                          │
│                                              │
│             接下来：现场演示                     │
│             （3 分钟）                         │
│                                              │
└──────────────────────────────────────────────┘
```

**内容细节**：
- 纯文字居中，42pt IKB 蓝 "感谢各位老师 请多指教"
- 下方 18pt 浅灰 "接下来：现场演示（3 分钟）"
- 右上角小面积网格点阵装饰
- 全页最大留白

**讲稿**：鞠躬后说 "接下来我做一个简单的现场演示。" 然后切换到系统界面。

---

## 三、完整叙事线

```
P1  封面（10"）         →  静默翻页，建立第一印象
     ↓
P2  问题（30"）         →  "企业有这么多重复浏览器操作，现有方案要么贵要么难"
     ↓
P3  核心创新（40"）      →  "所以我做了这个：画布×浏览器=低代码RPA"  ← 全场锚点
     ↓
P4  架构（25"）         →  "五层结构，界面和引擎解耦"
     ↓
P5  技术栈（15"）       →  "用的都是成熟技术，一页带过"
     ↓
━━━━━━━━━━━━ 重点段落开始 ━━━━━━━━━━━━
     ↓
P6  亮点①编排（30"）    →  "拖拽连线就能搭流程，不用写代码"
     ↓
P7  亮点②监控（30"）    →  "每一步运行状态实时可见，不是黑盒"
     ↓
P8  亮点③调试（30"）    →  "失败了能断点、能回溯、能远程操控"
     ↓
━━━━━━━━━━━━ 重点段落结束 ━━━━━━━━━━━━
     ↓
P9  数据节点（20"）     →  "不止点按，覆盖完整数据链路" ← 为演示铺垫
     ↓
P10 产品化（20"）       →  "市场、调度、认证、协作，首版闭环"
     ↓
P11 测试数据（20"）     →  "142 用例 97.2%，用数据说话"
     ↓
P12 设计决策（15"）     →  "三个关键技术选择" ← 含答辩预埋
     ↓
P13 总结（20"）         →  "完成了可编排、可执行、可调试、可协作的系统"
     ↓
P14 致谢（5"）          →  "感谢各位老师" → 过渡到演示
```

**情绪曲线**：
- P1-P2：建立问题认知（平稳开场）
- P3：抛出核心论点（第一次高点）
- P4-P5：技术铺垫（回落，快速带过）
- P6-P8：三大亮点连续输出（持续高能区，整场核心）
- P9-P11：证据收束（平稳回落，用数据锚定）
- P12-P14：收尾致谢（平缓下降）

---

## 四、风险点与应对

| 风险 | 应对 |
|------|------|
| P6-P8 需系统截图，开发环境不稳定 | 提前截好 3 张高清图（PNG，1440×900 以上），PPT 直接用静态图，不依赖实时系统 |
| 投影仪分辨率低，截图细节看不清 | 截图时放大关键区域，或 PPT 中额外放局部放大标注图 |
| 评委追问技术细节超出准备 | P12 已预埋四条常见追问话术；若偏离预埋范围，先用架构图 P4 重新建立共识再回答 |
| 5 分钟超时 | P5（技术栈）和 P9（数据节点）为弹性页——若已超时可一句话跳过，不影响叙事完整性 |
| 演示环节翻车 | 准备一段 30 秒的备用录屏视频，放在本地 |

---

## 五、素材清单（按优先级排列）

| 优先级 | 素材 | 用途 | 尺寸要求 |
|--------|------|------|----------|
| 🔴 必选 | 编辑器三栏布局截图 | P6 主体 | ≥1440×900 |
| 🔴 必选 | 运行监控面板截图 | P7 主体 | ≥1440×900 |
| 🔴 必选 | 调试工作台截图 | P8 主体 | ≥1440×900 |
| 🟡 建议 | 模板首页截图（含搜索+标签+卡片） | P10 辅助 | ≥1200×800 |
| 🟡 建议 | 系统架构简化图 | P4 主体 | 矢量 SVG |
| 🟡 建议 | 执行链路流程图 | P3 辅助 | 矢量 SVG |
| 🟢 可选 | 模板市场截图 | P10 辅助 | ≥1200×800 |
| 🟢 可选 | 演示备用录屏（30s） | 风险应对 | 1080p MP4 |
