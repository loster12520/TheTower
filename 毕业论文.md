# 东莞理工学院本科毕业设计

## 题目：基于Playwright的浏览器RPA画布工作流系统

学生姓名：XXX  
学号：XXX  
学院：计算机科学与技术学院（网络空间安全学院）  
专业班级：2022级计算机科学与技术1班  
指导教师姓名及职称：XXX副教授  
起止时间：2025年12月—2026年5月

## 摘要

随着企业数字化转型和个人办公效率提升，网页表单录入、信息采集、状态监控等重复性浏览器操作越来越常见。传统人工处理方式耗时长、易出错，难以适应高频、规则明确的业务场景；而商业化机器人流程自动化产品通常价格较高、配置复杂，对中小企业和普通业务人员并不友好。针对上述问题，本文设计并实现了一个基于Playwright的浏览器RPA画布工作流系统 TheTower，旨在通过低代码、可视化的方式降低浏览器自动化技术的使用门槛。

本系统采用前后端分离架构。前端基于React、Umi与ReactFlow 构建可视化流程编排界面，支持节点拖拽、连线、参数配置等编辑能力；后端基于Ktor与 Playwright Java构建执行与管理服务，负责模板持久化、运行调度、步骤解析、浏览器自动化执行以及事件推送。系统通过 REST 接口完成模板、运行等数据交换，通过 WebSocket 实时推送运行状态和调试事件，实现流程执行过程的可视化监控。

围绕“可编排、可执行、可调试、可扩展”的设计目标，系统在 0.1.0 版本中完成了编辑器体验完善、高级调试、模板市场、调度任务等首版能力闭环。其中，执行链路支持一系列多种节点类型；调试链路支持条件断点、时间旅行、远程操控和上下文查看；产品化能力支持模板分享、市场复用、多工作空间隔离和在线协作。

测试结果表明，系统已完成后端单元测试、接口烟雾测试、前端回归测试、真实后端联调测试。后端编译与前端编译均通过，模板管理、运行调试、数据文件节点、网络监听、市场、调度、认证和协作主线均完成验证。系统已完成 50 节点画布性能测量与回归资产沉淀，后端执行链路完成多浏览器兼容验证。本文的研究与实现为低代码浏览器自动化系统的设计提供了可落地的工程实践，也为后续协作冲突自动合并、细粒度权限和更强持久化能力的演进奠定了基础。

**关键词：** 浏览器RPA；可视化流程编排；Playwright；ReactFlow；低代码

## Abstract

With the digital transformation of enterprises and the improvement of personal office efficiency, repetitive browser operations such as web form entry, information collection, and status monitoring have become increasingly common. The traditional manual processing method is time-consuming and prone to errors, making it difficult to adapt to high-frequency and clearly-defined business scenarios; while commercial robot process automation products are usually expensive and complex in configuration, and are not friendly to small and medium-sized enterprises and ordinary business personnel. To address these issues, this paper designs and implements a browser RPA canvas workflow system called TheTower, aiming to lower the threshold for using browser automation technology through low-code and visual methods.

This system adopts a front-end and back-end separation architecture. The front-end is built using React, Umi, and ReactFlow to create a visual process orchestration interface, supporting node dragging, connection, parameter configuration, and other editing capabilities; the back-end is built using Ktor and Playwright Java to handle template persistence, run scheduling, step parsing, browser automation execution, and event pushing. The system completes data exchange through REST interfaces and real-time pushes the running status and debugging events through WebSocket to achieve visual monitoring of the process execution.

Centering on the design goals of "reconfigurable, executable, debuggable, and scalable", the system completed the first version capability loop of editor experience improvement, advanced debugging, template market, and scheduling tasks in version 0.1.0. Among them, the execution link supports a series of various node types; the debugging link supports conditional breakpoints, time travel, remote control, and context viewing; the productization capability supports template sharing, market reuse, multi-workspace isolation, and online collaboration.

The test results show that the system has completed backend unit testing, interface smoke testing, front-end regression testing, and real backend integration testing. Both backend compilation and front-end compilation passed, and the template management, run debugging, data file nodes, network monitoring, market, scheduling, authentication, and collaboration mainlines were all verified. The system has completed 50 node canvas performance measurement and regression asset accumulation, and the backend execution link completed multi-browser compatibility verification. The research and implementation of this paper provide a practical engineering practice for the design of low-code browser automation systems, and lay the foundation for the subsequent evolution of automatic conflict merging, fine-grained permissions, and stronger persistence capabilities.

**Keywords:** browser RPA, visual workflow orchestration, Playwright, ReactFlow, low-code

## 目录

第1章 绪论  
第2章 相关技术和开发环境  
第3章 系统设计  
第4章 概要设计  
第5章 详细设计  
第6章 系统实现  
第7章 系统测试  
第8章 总结与展望  
参考文献  
致谢

# 第1章 绪论

## 1.1 课题应用背景

在当前数字化办公环境中，大量业务流程已经迁移至浏览器端完成，例如后台信息录入、订单状态查询、运营数据采集、内容审核和页面监控等。这类任务通常具备规则固定、重复频率高、人工价值密度低等特征，若完全依赖人工处理，不仅效率较低，而且在长时间重复操作下容易产生误点、漏填和数据不一致等问题。尤其对于中小企业和非技术部门而言，这类工作虽然适合自动化，但缺少足够轻量、可负担、可维护的技术工具。

传统浏览器自动化通常依赖脚本开发完成，虽然 Selenium、Playwright 等自动化框架已经较为成熟，但其使用前提仍然是操作者具备编程能力，能够理解浏览器定位、元素等待、流程控制、异常处理和数据落盘等技术细节。另一方面，商业化 RPA 平台虽然提供了图形化编排界面，却普遍存在授权成本较高、场景适配封闭、定制能力受限等问题，不利于毕业设计场景下的研究与落地。因此，围绕浏览器操作构建一个面向普通用户的低代码、可视化自动化系统，具有明确的实践价值和研究意义。

本课题的目标是设计并实现一个基于 Playwright 的浏览器 RPA 画布工作流系统 TheTower。系统允许用户通过拖拽节点、配置参数和连线的方式在可视化画布中编排工作流，再由后端解析流程定义并驱动真实浏览器执行，同时将运行状态、调试事件和错误信息实时反馈给前端。这种设计既保留了自动化框架的执行能力，又通过流程建模和可视化编排降低了使用门槛，适合在低代码自动化场景中推广。

## 1.2 现有产品和存在问题

现有网页自动化解决方案大致可以分为两类。第一类是代码驱动型方案，例如直接使用 Playwright、Selenium 或 Puppeteer 编写自动化脚本。这类方案灵活度高、可控性强，适合复杂场景和深度定制，但开发与维护成本较高，普通业务人员很难直接上手。第二类是可视化 RPA 平台，它们通过拖拽方式降低使用门槛，适合标准化业务流程，但通常更偏向企业级交付，其部署、授权、插件体系和定制成本较高，对于轻量化教学项目和中小规模业务并不完全匹配。

综合来看，现有产品主要存在四个问题。第一，技术门槛高。脚本式自动化要求使用者掌握编程语言、选择器规则、调试方法和工程化知识。第二，成本较高。成熟商业平台往往依赖付费授权和企业级部署，超出了小团队和个人用户的承受范围。第三，执行过程不透明。许多轻量脚本方案虽然能运行，但难以为非技术用户提供清晰的运行状态、失败步骤、变量上下文和错误日志。第四，扩展能力割裂。部分平台对文件读写、网络监听、调试、协作和模板复用支持有限，难以形成完整的产品闭环。

针对上述问题，TheTower 选择“可视化编排 + 浏览器自动化引擎 + 实时状态反馈 + 产品化首版能力”这一组合路线。一方面，系统在前端引入 ReactFlow 作为节点式可视化画布，提供符合用户认知的拖拽式工作流设计体验；另一方面，系统以后端 Playwright 执行器承载真实浏览器操作，并通过 REST 与 WebSocket 形成统一的运行与调试协议。相较于单纯脚本方案，TheTower 更强调可视化、可观测和可复用；相较于重量级商业平台，TheTower 更强调轻量、可本地部署和工程可控。

## 1.3 论文的主要工作

本文围绕浏览器 RPA 画布工作流系统的设计与实现，完成了以下几方面工作。

第一，完成系统需求分析与总体架构设计。基于重复网页任务的自动化场景，明确系统应支持可视化流程编排、步骤执行、运行监控、调试排障、数据输入输出和协作共享等核心能力，并确定前后端分离架构以及 REST 与 WebSocket 双通道通信方案。

第二，完成前端可视化编排系统实现。系统基于 React、Umi、TypeScript 和 ReactFlow 构建画布编辑器，支持节点拖拽、连线、参数配置、自动保存、自动布局、模板搜索、标签分组、克隆和批量删除等高频操作，为非技术用户提供低代码编排能力。

第三，完成后端流程执行引擎实现。系统基于 Kotlin 与 Ktor 提供模板管理、运行管理、调试控制、调度、认证和协作等服务；基于 Playwright Java 实现浏览器自动化执行链路，支持打开网页、点击、输入、等待、提取、文件与 Excel 操作、剪贴板读取和请求监听等节点执行。

第四，完成运行监控与高级调试能力实现。系统通过 WebSocket 推送运行开始、步骤开始、步骤成功、步骤失败、日志和运行结束等事件，并在调试模式下支持条件断点、时间旅行、远程操控和上下文查看，提升复杂自动化流程的可排查性。

第五，完成 0.1.0 版本的产品化首版能力补齐。系统已形成模板市场、调度任务、认证与工作空间、协作编辑等首版闭环，使其从单纯的技术原型演进为具备初步平台属性的浏览器自动化系统。

第六，完成系统测试与文档收口。后端单元测试、前端构建验证、端到端回归、真实后端联调测试和文档验收均已完成，为版本收口与论文撰写提供了较充分的证据支撑。

## 1.4 章节安排

本文共分为八章，各章内容安排如下。

第一章为绪论，主要介绍课题背景、现有方案存在的问题、课题目标和本文主要工作。第二章介绍系统实现所涉及的相关技术与开发环境，包括 ReactFlow、Playwright、Ktor、React/Umi 和 SQLite 等。第三章从可行性、业务需求和功能需求角度分析系统设计基础。第四章介绍系统概要设计，包括总体架构、模块划分、数据模型和接口设计。第五章重点展开系统详细设计，对前端编排、后端执行、通信协议、调试与协作链路进行说明。第六章说明系统实现结果与关键功能落地情况。第七章给出测试环境、测试方法、测试内容和结果分析。第八章总结全文，并对系统未来演进方向进行展望。

# 第2章 相关技术和开发环境

## 2.1 相关技术与相关研究调研

浏览器自动化与低代码平台的结合，是近年来软件工程和业务自动化领域的一个重要方向。浏览器自动化技术的核心目标，是通过程序驱动浏览器完成页面加载、元素定位、事件触发、数据提取和结果保存等操作，从而替代人工完成重复性网页任务。低代码平台则强调将复杂的技术流程抽象为图形化配置，以降低使用者的技术门槛。将二者结合，能够在兼顾灵活性的同时，改善自动化系统的可用性和可维护性。

在本课题中，系统研究重点并不在于重新发明底层浏览器自动化机制，而在于如何把成熟自动化框架封装为面向普通用户的可视化工作流系统。因而，课题的关键技术路线包括三个方面：一是节点式可视化编排技术，解决流程建模和参数配置问题；二是浏览器自动化执行技术，解决真实页面控制和数据提取问题；三是前后端协同与实时事件推送技术，解决流程执行可观测性问题。围绕这三条技术主线，本文最终选择 ReactFlow、Playwright、Ktor 与 SQLite 作为系统实现基础。

## 2.2 相关技术1：ReactFlow 可视化编排技术

ReactFlow 是一套面向节点式应用的前端图形库，适用于流程图编辑器、数据流编排器和低代码工作台等场景。其核心优势在于内置节点与边模型、拖拽与缩放支持、小地图、选择与多选、连线管理和布局扩展能力，能够显著降低可视化编排界面的实现成本。对于本课题而言，ReactFlow 不仅提供了画布渲染能力，更重要的是为“节点 + 参数 + 连线”的流程建模提供了天然契合的数据结构。

TheTower 将 ReactFlow 作为前端画布基础，构建了步骤库、画布区和配置面板的三栏式编辑结构。每个节点以固定类型和配置 schema 定义，用户拖拽节点到画布后，可通过侧边栏填写 URL、选择器、文本、变量名、文件路径和监听规则等参数。ReactFlow 提供的节点与边管理能力使系统可以较方便地维护主流程步骤与零散画布节点之间的关系，为后续模板保存、导入导出和自动布局提供了基础。

## 2.3 相关技术2：Playwright 浏览器自动化技术

Playwright 是现代浏览器自动化框架，支持 Chromium、Chrome、Firefox、WebKit 和 Edge 等多种浏览器或通道，具备智能等待、稳定元素定位、页面上下文控制和网络请求监听等能力。与早期自动化方案相比，Playwright 在现代前端页面的兼容性、等待机制和跨浏览器支持方面具有明显优势，因此非常适合作为浏览器 RPA 系统的执行内核。

TheTower 后端基于 Playwright Java 构建执行器，将前端编排的步骤树映射为浏览器操作序列。系统通过打开网页、点击、输入、等待、文本提取、属性提取、数据保存、Excel 导入导出和请求监听等执行分支，实现对真实业务页面的自动化控制。同时，Playwright 的调试能力与浏览器上下文管理能力，也为系统扩展条件断点、时间旅行、远程操控和调试预览提供了技术基础。

## 2.4 相关技术3：Ktor 与前后端协同通信技术

Ktor 是基于 Kotlin 的轻量级异步 Web 框架，具备较好的模块化能力和协程支持，适合构建中小规模但结构清晰的 REST 与 WebSocket 服务。在本课题中，Ktor 承担模板管理、运行管理、调试控制、调度任务、认证与协作等接口服务职责，并与 Kotlin Coroutines 配合，为每个运行任务分配独立协程，支持启动、取消、重启和调试控制。

系统通信分为两类。第一类是 REST 接口，负责模板 CRUD、运行创建、调度维护、市场发布导入、用户登录和协作分享等具备明确请求响应边界的业务场景。第二类是 WebSocket 事件流，负责运行开始、步骤开始、步骤成功、步骤失败、日志、运行结束、调试状态和协作补丁等实时事件的推送。REST 与 WebSocket 的组合使系统既能保证管理接口的清晰性，又能提供执行过程的实时可视化能力。

## 2.5 开发环境

### 2.5.1 软件环境

本课题主要的开发环境如下表所示：

本课题开发环境以现代前后端分离项目常用工具链为基础。前端使用 React 18、Umi 4、TypeScript、MobX、Ant Design 与 ReactFlow 构建界面与状态管理；后端使用 Kotlin 2.2.20、Ktor 2.3.12、Playwright Java、Kotlinx Serialization、SQLite JDBC 和 Logback 实现服务与执行器。开发过程中，前端使用 npm 进行依赖管理与构建，后端使用 Gradle 管理依赖、测试与打包。

系统默认通过本地方式部署运行。后端默认监听 8080 端口，SQLite 数据文件默认位于 backend/data/thetower.db；浏览器执行器默认使用 chromium，默认无头模式开启，默认步骤超时为 10000 毫秒。系统支持通过环境变量覆盖浏览器类型、无头模式和步骤超时，便于不同调试与测试场景切换。

本系统使用到的主要开源包如下表所示：

- ktor: 2.3.12
- playwright-java: 1.6.3
- jimmer: latest.release
- sqlite-jdbc: 3.46.1.3
- react: 19.2.10
- xyflow/react(react-flow): 12.10.0
- ant: 6.2.2
- axios: 1.13.4
- mobx: 6.15.0
- umi: 4.6.25

### 2.5.2 硬件环境

由于系统本质上是浏览器自动化工作流平台，其运行环境需要同时承担前端界面渲染、后端服务执行和浏览器进程运行等任务，因此硬件环境应具备基本的多任务运行能力。开发与测试中，建议处理器为主流双核及以上配置，内存建议 8GB 及以上，以保证运行多个浏览器实例或并发流程时的稳定性。若需要进行真实浏览器调试、长时间自动化回归或多流程并发测试，则更高的处理器和内存配置会带来更稳定的体验。

# 第3章 系统设计

## 3.1 可行性研究

系统设计之前需要从经济、技术和运行三个角度评估课题可行性。TheTower 的设计目标不是构建一个面向超大规模企业场景的商业平台，而是实现一个可验证、可扩展、可演示的浏览器自动化工作流系统。因此，可行性分析更强调技术选型是否成熟、系统复杂度是否可控、项目边界是否适合毕业设计周期，以及最终系统是否具备清晰的交付价值。

### 3.1.1 经济可行性

本系统整体采用开源技术栈实现，前端使用 React、Umi、TypeScript 与 ReactFlow，后端使用 Kotlin、Ktor、Playwright 和 SQLite，开发、测试与部署均可在普通个人电脑上完成，不依赖昂贵的商业授权。对于毕业设计项目而言，这种技术方案能够在较低成本下获得较高的功能完整度，同时保留较好的可移植性与可扩展性。从后续推广角度看，系统也更适合面向中小企业或个人用户以本地部署方式使用，因此经济上具备较高可行性。

### 3.1.2 技术可行性

本系统所依赖的关键技术均为成熟方案。ReactFlow 已广泛应用于节点式编辑器开发，能够支撑画布拖拽、连线和节点配置；Playwright 具备稳定的浏览器自动化能力和跨浏览器支持；Ktor 与 Kotlin 协程适合构建结构清晰、并发友好的后端服务；SQLite 则满足本课题本地持久化需求。项目文档与测试报告显示，当前系统已完成多浏览器兼容验证、50 节点画布性能验证、后端单元测试与真实后端联调，因此技术路线已经从理论可行推进到工程可行。

### 3.1.3 运行可行性

从运行角度看，TheTower 的目标用户并不是专业开发人员，而是希望通过图形化方式快速配置自动化流程的业务人员或开发者。系统通过步骤库、画布、配置面板和运行监控面板组织用户操作流程，使用上符合“拖拽节点、配置参数、连接流程、运行与调试”的直观认知。系统还补齐了自动保存、搜索、克隆、调试和协作等高频需求，降低了连续使用中的学习成本和操作成本，因此在运行层面具备较高可行性。

## 3.2 业务需求分析

业务需求分析的核心是明确用户为什么需要该系统以及系统要服务哪些典型场景。TheTower 面向的主要场景是浏览器端的重复性规则任务，例如后台数据录入、网页信息采集、页面监控、结果导出和接口响应观察等。这些场景通常具有三个特征：业务流程可抽象为离散步骤；执行顺序明确且经常重复；在运行过程中需要记录状态、结果和错误信息。传统人工操作虽然容易上手，但随着任务频率增加，效率与准确性都会明显下降。

从用户角度看，系统主要应解决五类业务需求。第一，用户需要通过低代码方式搭建自动化流程，而不是直接编写脚本。第二，用户需要能够复用已有流程模板，并对模板进行搜索、分组、标签管理、克隆和删除。第三，用户需要在执行前设置浏览器、超时、无头模式等运行参数，并在执行中实时查看进度、日志和产物。第四，用户需要通过调试能力定位失败步骤和上下文问题，减少自动化流程维护成本。第五，随着使用深入，用户还需要模板共享、定时触发、市场复用和多人协作等产品化能力。

## 3.3 功能需求分析

结合项目需求文档与用户手册，可以将 TheTower 的功能需求划分为四个层面。

第一层为编排与模板管理功能。系统应支持新建模板、编辑模板、保存模板、删除模板、克隆模板、导入导出模板，并支持名称搜索、标签分组和批量删除。编辑器应支持节点拖拽、连线、复制粘贴、缩放、小地图、自动布局和自动保存。

第二层为执行与调试功能。系统应支持普通运行和调试运行两类模式。普通运行下，系统应能按步骤顺序执行工作流并输出日志和结果；调试运行下，系统应支持条件断点、继续执行、单步执行、上下文查看、时间旅行和预览远程操控等能力，以帮助用户定位复杂问题。

第三层为数据和集成功能。系统应支持文件与 Excel 节点、剪贴板节点、焦点元素节点和请求监听节点，将运行期数据写入变量或落盘保存。文件类节点需要定义输入路径、输出路径和失败反馈，网络监听节点则需要定义监听范围、监听生命周期和命中结果保存方式。

第四层为产品化功能。系统应支持模板市场发布与导入、定时调度、认证登录、工作空间切换、模板分享和协作编辑。对于 0.1.0 版本而言，这些功能不再只是界面入口，而是已经形成“前端入口 + 后端协议 + 存储链路 + 测试证据”的首版闭环。

# 第4章 概要设计

## 4.1 系统设计

### 4.1.1 系统架构设计

TheTower 采用前后端分离架构。前端负责工作流可视化编排、模板展示、运行监控和用户交互；后端负责模板持久化、运行管理、调试控制、调度、协作和浏览器自动化执行。两者通过 REST 接口进行标准化数据交互，通过 WebSocket 实现运行事件、调试事件和协作事件的实时同步。该架构把“界面编排”与“实际执行”进行解耦，使前端可以专注于图形建模与交互体验，后端可以专注于流程编译、状态管理和自动化执行。

从执行链路看，用户首先在前端画布中完成工作流编排，系统将节点与连线组织为流程定义后保存为模板。运行开始时，前端通过 POST /api/v1/runs 接口发送运行请求，并附带模板 ID、浏览器参数和调试参数。后端 RunService 创建运行记录后，调用 PlaywrightRunExecutor 打开浏览器上下文，并在 PlaywrightRunSession 中逐步执行步骤树。在执行过程中，系统持续向 WebSocket 通道推送 RUN_STARTED、STEP_STARTED、STEP_SUCCEEDED、STEP_FAILED、LOG、RUN_SUCCEEDED 等事件，使前端能够实时呈现执行过程。

### 4.1.2 系统功能模块

从功能划分看，系统主要由六个模块构成。

第一，模板管理模块。该模块负责模板创建、编辑、查询、更新、删除、克隆、批量删除以及模板市场发布和导入。第二，流程编排模块。该模块负责节点库展示、节点拖拽、连线关系维护、参数配置和自动布局。第三，运行与调试模块。该模块负责普通运行、调试运行、断点控制、时间旅行、预览远程操控和日志查看。第四，数据与集成模块。该模块负责文件、Excel、剪贴板、焦点元素和请求监听节点的执行与结果处理。第五，平台能力模块。该模块负责调度任务、登录认证、工作空间与协作编辑。第六，协议与存储模块。该模块负责 REST/WS 协议、SQLite 数据持久化、运行产物与协作数据落盘。

各模块之间并不是孤立工作的，而是通过统一数据模型和协议相互协同。例如，模板管理模块维护 WorkflowTemplate，流程编排模块修改模板中的 steps 与 otherStep，运行与调试模块消费模板数据生成 Run 记录，平台能力模块则通过模板和工作空间建立访问边界。这样的模块划分既有利于前后端职责清晰，也有利于后续扩展新节点和新能力。

## 4.2 系统核心功能设计

### 4.2.1 关键概念定义

系统中的核心概念主要包括模板、步骤、运行、调试会话、工作空间和协作会话。模板是工作流的持久化载体，用于描述某一自动化流程的结构、元数据和最近运行摘要。步骤是模板中的最小可执行单元，每个步骤具有 type 与 config 两部分，分别表示节点类型和参数配置。运行是某次模板执行的实例，保存运行状态、当前步骤、开始时间、结束时间和错误信息等内容。调试会话是运行的特殊态，用于支持暂停、继续、单步和远程操控等行为。工作空间用于隔离不同用户或团队的模板资源，协作会话则用于管理模板的共享、在线成员状态和补丁同步。

### 4.2.2 模型建立

系统的模板模型 WorkflowTemplate 主要包含 id、name、description、schemaVersion、steps、otherStep、createdAt、updatedAt、stats 和 lastRun 等字段。其中，steps 是主流程步骤的有序数组，直接决定执行顺序；otherStep 用于保存零散节点与边，更偏向画布视图层面的补充结构。Run 模型用于表示某次运行实例，其 status 枚举包括 PENDING、RUNNING、SUCCEEDED、FAILED 和 CANCELED。系统通过该模型把“流程定义”和“流程运行实例”区分开来，从而实现模板复用与多次执行。

在事件模型方面，系统定义了运行事件、调试事件和协作事件三类消息。运行事件包括 RUN_STARTED、STEP_STARTED、STEP_SUCCEEDED、STEP_FAILED、LOG、RUN_SUCCEEDED、RUN_FAILED 和 RUN_CANCELED；调试事件包括 DEBUG_SESSION_STARTED、DEBUG_FRAME、DEBUG_BREAKPOINT_HIT、DEBUG_CONTEXT_UPDATED、DEBUG_STEPPED 等；协作事件包括在线成员变化和补丁应用通知。统一的事件模型使前端可以通过同一事件管道感知不同执行阶段的系统状态。

### 4.3 系统接口设计

系统接口分为模板接口、运行与调试接口、模板市场接口、调度接口、认证与工作空间接口以及协作接口六大类。模板接口包括 GET /templates、POST /templates、PUT /templates/{id}、PATCH /templates/{id}、POST /templates/{id}/clone 和 POST /templates/batch-delete 等，用于完成模板的生命周期管理。运行接口包括 POST /runs、GET /runs、GET /runs/{id}、POST /runs/{id}/cancel 和 POST /runs/{id}/restart 等。调试接口则扩展了 open-browser、continue、step、close 和 remote-control 等行为，支持调试态下的浏览器控制。

平台能力接口中，模板市场接口支持发布与导入；调度接口支持单次任务与周期任务配置；认证接口支持登录、登出、当前用户信息与工作空间列表查询；协作接口支持模板分享、presence 心跳和在线成员快照。所有 REST 接口统一采用 /api/v1 作为前缀，响应采用 requestId、data 与 error 的包装格式。WebSocket 方面，运行事件通道为 /ws/v1/runs/{runId}，协作事件通道为 /ws/v1/templates/{templateId}/collaboration。这种统一协议设计有利于前后端联调和测试资产复用。

# 第5章 详细设计

## 5.1 流程编排模型与前端交互设计

前端编排模块以 ReactFlow 为核心，围绕“步骤库、画布、配置面板”三个区域组织用户交互。步骤库负责展示可用节点类型，画布负责节点的放置、拖拽、连线和布局，配置面板则负责节点参数和运行参数的输入。系统通过 stepRegistry 维护节点注册表，每个节点都具有固定的 type、label、group、icon 和表单 schema，从而保证节点配置的一致性和可扩展性。当前系统支持的节点类型已经覆盖页面操作、等待、数据提取、文件与 Excel 处理、剪贴板、焦点元素和网络监听等多种类别。

在画布交互方面，系统不仅支持基础拖拽和连线，还支持复制粘贴、撤销重做、缩放、小地图和自动布局。为了降低操作成本，0.1.0 版本补齐了自动保存机制，使编辑结果能够在一定周期内自动写回后端，同时提供状态提示避免用户误判保存结果。首页模板管理也增加了搜索、标签分组、克隆和批量删除等能力，使模板数量增长后仍然便于维护。前端对这些能力的组织方式，体现了系统从“可编辑”向“可长期使用”转变的设计思路。

## 5.2 后端执行引擎设计与实现

后端执行链路由 RunService、PlaywrightRunExecutor 和 PlaywrightRunSession 共同组成。RunService 负责接收运行请求、创建 Run 记录、生成 WebSocket 事件流、启动或取消执行协程，并在必要时创建调试会话。PlaywrightRunExecutor 负责解析浏览器类型与启动参数，创建对应浏览器与上下文。PlaywrightRunSession 则负责实际的步骤执行主循环，根据步骤类型分发到不同执行分支，并在执行过程中维护变量上下文、日志输出、页面状态和错误处理。

执行开始后，系统首先发送 RUN_STARTED 事件，随后按照 steps 数组顺序逐步执行节点。每个步骤执行前发送 STEP_STARTED 事件，执行成功后发送 STEP_SUCCEEDED 事件，若失败则发送 STEP_FAILED 事件并附带错误码与错误信息。对于需要产物输出的节点，例如 saveData、saveExcel 和 importExcel，系统会将结果写入运行数据目录或变量上下文，供后续步骤继续使用。对于 listenRequestTrigger 和 listenRequestResult 等节点，系统会在浏览器会话中注册监听器，并在命中请求后把结果写入变量，从而让网络事件可以被后续步骤消费。

该执行器设计的关键点在于把“抽象节点”映射为“具体浏览器操作”。例如，openUrl 被映射为页面打开行为，click 被映射为元素点击行为，type 被映射为定位并输入文本，extract 则根据 mode 决定提取 innerText 或 attribute。通过统一节点类型和参数结构，系统能够在不暴露 Playwright 细节的前提下提供较稳定的自动化能力。这也是本课题区别于直接编写脚本的重要设计选择。

## 5.3 调试机制与运行时上下文设计

复杂自动化流程在真实页面中运行时，常会受到元素变化、网络延迟、变量值异常和步骤配置错误等因素影响，因此调试能力是系统可维护性的关键。TheTower 在普通运行之外提供调试运行模式，并设计了以 DebugExecutionGate 和调试事件流为核心的调试机制。调试参数中包含是否启用调试、是否打开可见浏览器、是否打开 DevTools、预览帧率、预览质量、是否启动后暂停、断点列表和结束后是否保留浏览器等字段，保证普通运行和调试运行在协议语义上保持一致。

在调试运行过程中，系统可以根据条件断点或命中次数条件暂停执行，并向前端发送 DEBUG_BREAKPOINT_HIT、DEBUG_CONTEXT_UPDATED 和 DEBUG_FRAME 等事件。前端调试工作台可据此展示当前步骤、变量上下文、预览画面和运行轨迹，用户可选择继续执行、单步执行或通过 remote-control 接口在调试预览中发送点击、输入等指令。时间旅行功能则用于回看关键步骤、变量快照和运行轨迹的历史状态，帮助用户快速理解失败前后的执行过程。

运行时上下文设计不仅包含变量和值，还包含页面会话、产物路径、调试状态和监听器注册等信息。执行某些节点时，系统会把提取结果或请求监听结果写入变量空间，供后续条件分支、循环或保存节点使用。这样一来，系统完成了从页面操作、数据提取到条件决策的闭环，使自动化流程具备更高的表达能力。

## 5.4 协议、协作与数据持久化设计

TheTower 的协议设计遵循“REST 管理 + WebSocket 事件”的双轨方案。REST 用于处理清晰的请求响应操作，例如创建模板、查询运行列表、创建调度、登录和分享模板。WebSocket 用于推送运行状态、调试信息和协作变更。这种设计一方面简化了前端状态管理，另一方面增强了执行过程的实时性。对于浏览器自动化系统而言，单纯依赖轮询很难以低延迟方式展示步骤级别状态，而事件流机制能够更自然地支持运行监控与调试工作台。

协作设计方面，系统增加了模板分享、在线成员快照、presence 心跳、协作事件流和差异采纳能力。用户在模板卡片中可配置分享对象与权限，进入编辑器后可查看在线成员和远端保存提示，当出现冲突时可查看差异摘要、差异详情，并选择性采纳节点或连线变更。当前协作能力已形成首版闭环，但复杂多方并发场景下的细粒度自动合并仍属于后续优化方向。

数据持久化方面，系统主要使用 SQLite 保存核心业务数据，并把运行产物、协作数据等以文件方式落盘。SQLite 满足本课题本地单体部署、快速开发和低运维成本的目标；文件持久化则适合存放运行日志、导出数据和协作补丁等附属内容。这样的设计使系统能够在毕业设计规模下保持实现简洁，同时为后续向更强持久化与审计能力演进预留空间。

# 第6章 系统实现

## 6.1 模板管理与编辑器实现

系统首页已实现模板创建、编辑、删除、导入导出、搜索、标签分组、克隆和批量删除等能力。用户进入首页后可通过“新建模板”创建工作流模板，也可使用关键字搜索和标签分组对模板进行筛选。对于已有模板，可通过克隆快速生成副本，避免重复搭建基础流程。批量删除功能则用于集中清理无效模板，提高管理效率。上述能力在 0.1.0 版本中已经完成前后端接口与回归测试闭环。

编辑器页面采用三栏式布局。左侧为步骤库，中间为 ReactFlow 画布，右侧为节点配置与运行参数面板。用户完成拖拽、连线和参数填写后，系统会通过自动保存机制持续同步编辑内容，从而降低手动保存遗漏风险。编辑器还提供自动布局能力，能够在主流程和基础分支场景下快速整理节点位置，使画布结构更加清晰。这些体验增强功能虽然不直接参与浏览器执行，但决定了系统是否适合长期使用，因此在本版本中被作为重要实现项完成。

## 6.2 执行与调试功能实现

在执行层面，系统实现了普通运行与调试运行双模式。普通运行模式下，用户可以在运行前设置浏览器类型、headless 和默认超时等参数，随后发起运行并查看实时状态、日志和产物。调试运行模式下，用户可进一步启用可见浏览器、DevTools、断点列表、预览帧率和暂停启动等参数。执行器会根据这些参数创建对应浏览器环境，并在必要时暂停流程等待用户操作。

调试工作台已经形成可用闭环。用户可通过继续执行、单步执行、上下文查看和远程操控等操作定位问题。系统支持条件断点、时间旅行和调试预览首版能力，能够在失败场景中更准确地定位步骤、变量与页面状态变化。相较于只给出“执行失败”这种粗粒度反馈，TheTower 把失败步骤、错误信息、上下文变量和调试操作统一纳入一套工作台中，显著提升了问题排查效率。

## 6.3 数据与网络监听节点实现

在节点能力扩展方面，系统实现了 saveData、saveExcel、importExcel、getClipboardText、extractActiveElement、listenRequestTrigger、listenRequestResult 和 stopPageListen 等节点。saveData 和 saveExcel 用于将运行期结构化数据输出为文件，importExcel 用于读取外部 Excel 数据参与执行；getClipboardText 可用于读取运行设备上的剪贴板内容，extractActiveElement 则可在页面交互场景中获取当前焦点元素信息。网络监听节点支持监听请求触发或响应结果，并将命中结果写入变量，供后续节点复用。

这些节点的实现拓宽了系统的应用边界。自动化流程不再局限于页面点击和文本输入，而是能够进行更完整的数据输入输出与运行时观测。例如，用户可以先导入 Excel 数据，再循环执行页面录入任务；也可以在页面请求触发时截取响应结果并保存文件。此类能力使系统更接近真实业务自动化场景，而不仅仅是教学演示。

## 6.4 模板市场、调度、认证与协作实现

为了让系统具备产品化首版能力，0.1.0 版本补齐了模板市场、调度任务、认证与工作空间以及协作编辑功能。模板市场支持浏览、发布和导入模板，降低了模板复用成本。调度任务支持单次任务和间隔任务，用户可在首页创建调度并查看最近触发结果。认证与工作空间功能用于区分不同用户和不同工作空间下的资源访问边界，使系统不再局限于单用户本地工具。

协作编辑是本版本的重要增强点。系统支持模板分享、在线成员展示、presence 心跳、远端保存同步、冲突提示和差异采纳。用户可以将模板分享给其他协作者，并在编辑器中实时感知在线成员和远端更新。当本地修改与远端修改冲突时，系统能够展示差异摘要和节点级差异内容，帮助用户做出选择。虽然当前协作能力仍属于首版实现，但它已经让系统具备了明显的平台化特征。

# 第7章 系统测试

## 7.1 测试环境和测试方法

系统测试覆盖后端测试、前端验证、端到端验证和文档交付验收四个维度。后端侧主要通过 Gradle 测试和服务级测试类验证模板服务、运行服务、调试执行门、市场、调度、认证与协作等核心逻辑；前端侧主要通过构建验证和 Playwright E2E 用例验证编辑器、模板管理、调试 UI 和平台入口；端到端测试侧通过真实后端联调和数据文件、网络监听节点回归验证完整闭环；文档验收则根据用户手册、API 文档和部署文档复现操作链路，确保项目交付材料与实际系统一致。

从测试方法看，系统综合采用单元测试、集成测试、回归测试、端到端测试和文档复现测试。单元测试用于验证关键类的逻辑正确性，例如执行器浏览器适配、模板服务和调试门控逻辑；集成与回归测试用于验证模板管理、运行调试和平台能力入口是否稳定；端到端测试用于验证真实浏览器和真实后端环境下的可用性；文档测试则用于检验交付材料是否真实可复现。多层次测试方式保证了系统不仅“功能存在”，而且“能够持续工作”。

## 7.2 测试用例设计和测试方案

根据测试报告，后端验证包括模板体验增强接口、运行参数与调试协议、数据文件与网络监听节点、调度任务接口、模板市场接口、认证与工作空间、协作与在线状态等七类核心用例，均已通过。相关证据包括 TemplateServiceTest、DebugExecutionGateTest、PlaywrightRunExecutorTest、StepTreeSupportTest、SchedulerServiceTest、TemplateMarketServiceTest、AuthServiceTest、TemplateCollaborationServiceTest 和 TemplatePresenceServiceTest 等测试资产。

前端验证包括自动保存与体验增强、搜索标签克隆批量删除、运行参数面板、高级调试 UI、模板市场入口、调度任务入口以及账户权限与协作入口等七类用例。端到端验证则围绕体验增强闭环、高级调试闭环、数据文件与网络监听闭环、产品化能力闭环四大场景展开。除此之外，系统还保留了 backend/scripts/api-smoke-test.ps1 和 backend/scripts/ws-event-test.ps1 两类脚本，用于在本地快速验证 REST 与 WebSocket 链路。

针对性能与兼容性，系统重点围绕 50 节点画布性能和多浏览器执行稳定性设计测试方案。前端性能测试关注画布渲染时间、节点选择响应和拖拽流畅度；后端兼容性测试关注 Chromium、Firefox、WebKit 及 Chrome、Edge 通道的执行适配。由于浏览器 RPA 系统通常要面对真实页面环境和长时间执行问题，因此这些测试内容对毕业设计成果的可用性评价具有较高价值。

## 7.3 结果和分析

测试结果显示，0.1.0 版本已经形成“实现证据 + 回归证据 + 文档证据”的完整闭环。后端 gradle test 已执行并通过，前端 npm run build 已执行并通过；模板管理、运行调试、数据文件节点、网络监听节点、市场、调度、认证与协作主线均已通过验证；历史缺陷如模板市场兼容、调度 404、协作冲突提示缺失和真实联调启动抖动等问题也已完成修复与回归。

从性能结果看，系统已完成 50 节点画布性能测量与回归资产沉淀，画布渲染、节点选择与拖拽在目标规模内保持稳定。需求文档中提出的“支持至少 50 个节点流畅拖拽与渲染”和后端“支持 Chrome、Firefox、Edge 三款浏览器自动化、单流程执行成功率大于等于 95%、支持并发执行至少 3 个流程”等指标，已经通过现有验证基线获得较充分支撑。测试结果说明，TheTower 不仅在功能上达成了毕业设计目标，也在工程稳定性和产品闭环方面具备较好的基础。

# 第8章 总结与展望

## 8.1 总结

本文围绕“降低浏览器自动化门槛”这一目标，设计并实现了一个基于 Playwright 的浏览器 RPA 画布工作流系统 TheTower。系统采用前后端分离架构，前端通过 ReactFlow 提供低代码可视化编排能力，后端通过 Kotlin、Ktor 和 Playwright 提供执行、调试、调度、认证和协作等核心能力，并通过 REST 和 WebSocket 实现管理操作与实时状态反馈。相比传统脚本式自动化方案，TheTower 更强调可视化、可观测和可扩展；相比重量级商业 RPA 平台，TheTower 更强调轻量、低成本和本地可部署。

从功能结果看，系统已在 0.1.0 版本中完成体验增强、高级调试、数据文件与网络监听节点、模板市场、调度任务、认证与工作空间、协作编辑等首版闭环。项目不仅实现了工作流编排与浏览器执行，还补齐了运行监控、问题排查、模板复用和多人协作等真实使用场景中不可缺少的能力。从测试结果看，系统已完成后端单元测试、前端构建验证、回归测试、真实联调测试和文档验收，表明项目已经从“可运行原型”提升到“可验证交付”的阶段。

从毕业设计意义看，本课题的价值主要体现在两个方面。其一，系统以工程方式验证了 ReactFlow 与 Playwright 结合构建低代码浏览器自动化平台的可行性。其二，系统围绕实际使用路径补齐了调试、数据节点和平台能力，使其不再只是技术展示，而具有一定的产品雏形。这些成果说明，浏览器自动化系统完全可以在保持执行能力的同时，以更友好的方式服务普通用户。

## 8.2 展望

虽然 TheTower 已完成 0.1.0 首版收口，但系统仍存在进一步增强空间。首先，协作能力目前已支持分享、在线状态和差异采纳，但复杂多方并发场景下的自动合并策略仍不够成熟，后续可以围绕操作级补丁、冲突分类和更直观的状态机可视化继续优化。其次，权限能力当前主要覆盖工作空间和模板分享，未来可扩展到更细粒度的操作授权、审计日志和角色模型，以适应更复杂的团队协作需求。

再次，调试能力已经具备条件断点、时间旅行和远程操控首版能力，但端到端自动化覆盖仍可继续加强，尤其是对长流程、复杂分支和异常恢复场景的验证。最后，系统当前以 SQLite 和本地文件存储为主，适合毕业设计和轻量部署场景；若进一步面向更大规模业务，可引入更强的数据库、对象存储和运维审计体系，以提升持久化能力和部署弹性。

总体来看，TheTower 已为浏览器 RPA 低代码化探索提供了完整的工程样本。未来若持续沿着协作增强、权限细化、调试强化和持久化升级方向演进，系统有望在教学研究之外形成更强的实际应用价值。

# 参考文献

[1] React Flow Documentation Team. React Flow Documentation: Learn how to build node-based applications[EB/OL]. https://reactflow.dev/learn.

[2] Microsoft. Playwright Documentation: Reliable end-to-end testing for modern web apps[EB/OL]. https://playwright.dev/docs/intro.

[3] JetBrains. Ktor Documentation: Build asynchronous servers and clients in Kotlin[EB/OL]. https://ktor.io/docs/welcome.html.

[4] 徐小夕. React-Flow中文文档正式上线: 指南[EB/OL]. https://www.cnblogs.com/gccbuaa/p/19205817.

[5] ycfenxi. ReactFlow: 构建交互式节点流程图的完全指南[EB/OL]. https://www.cnblogs.com/ycfenxi/p/19115406.

[6] 霍格沃兹测试学社. Playwright UI自动化实战速成指南[EB/OL]. https://bbs.huaweicloud.com/blogs/462395.

[7] TheTower 项目文档组. TheTower API 文档（0.1.0）[Z]. 项目文档.

[8] TheTower 项目文档组. TheTower 用户使用手册（0.1.0）[Z]. 项目文档.

[9] TheTower 项目文档组. TheTower 0.1.0 技术实现报告[Z]. 项目文档.

[10] TheTower 项目文档组. TheTower 0.1.0 测试报告[Z]. 项目文档.

# 致谢

本次毕业设计的完成，离不开指导教师在选题、方案设计、实现推进和论文撰写过程中的持续指导。老师在系统架构、论文结构和工程取舍方面给予了大量建议，使我能够在毕业设计周期内逐步完成从需求分析、系统实现到测试收口的完整工作。在此向指导教师表示衷心感谢。

同时，也感谢在项目开发和学习过程中给予我帮助的同学与朋友。无论是在技术讨论、测试反馈还是资料整理方面，这些帮助都使我能够更高效地推进课题研究。最后，感谢学校和学院提供的学习环境与实践平台，使本次毕业设计得以顺利完成，并为大学阶段的学习生活画上阶段性句号。