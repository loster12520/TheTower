# TheTower 毕业设计答辩汇报稿

---

## P1 · 封面（不讲解，直接翻页）

> *（站定，面向评委，点头致意，翻页）*

---

## P2 · 问题与动机（约 30 秒）

各位老师好，我做的课题是 **「基于 Playwright 的浏览器 RPA 画布工作流系统」**。

先看问题。企业里大量重复性的浏览器操作——**表单录入、信息采集、状态监控**——目前两条路：

- **要么写脚本**：Selenium、Playwright，灵活但 *需要编程基础*，业务人员用不了
- **要么买商业 RPA**：UiPath、来也，功能全但 *授权昂贵、配置复杂*，小团队买不起也改不动

所以我的课题方向很明确：**能不能把浏览器自动化的门槛降下来，同时保留足够的执行能力？**

---

## P3 · 核心创新（约 40 秒）★ 全场锚点

核心思路一句话概括——

> **ReactFlow 可视化画布 × Playwright 浏览器执行 = 低代码 RPA**

具体分三步走：

- **左边**：用户在画布上 *拖拽节点、配置参数、连线编排*，流程就搭好了（前端 ReactFlow）
- **中间**：后端拿到流程定义，用 Playwright 驱动 *真实的浏览器* 一步步执行（Playwright Java）
- **右边**：运行过程中的 *状态、日志、错误*，全部通过 WebSocket 实时推回前端

最终效果：**用户不需要写一行代码，只要理解自己的业务流程就行。**

---

## P4 · 系统架构总览（约 25 秒）

这个思路落地后，整体的系统结构是这样的——**标准前后端分离，五层架构**：

1. **前端展示层**：React + ReactFlow，负责画布编排和运行监控
2. **通信协议层**：REST 做管理，WebSocket 做实时推送
3. **后端服务层**：Ktor，负责模板、运行、调试、调度、认证、协作
4. **执行引擎层**：Playwright，步骤解析 + 真实浏览器驱动
5. **数据存储层**：SQLite + 文件存储

一句话总结：**界面管编排，后端管执行，两边通过双通道协议解耦。**

---

## P5 · 技术选型一览（约 15 秒，快速带过）

技术选型我快速带过。前端 **React + ReactFlow** 搭画布，**Umi** 做工程化；后端 **Ktor + Playwright** 做执行；**SQLite** 做存储。全是成熟方案，不展开。

---

## P6 · 亮点①：可视化编排（约 30 秒）★

下面进入三个核心亮点。**第一，可视化编排。**

打开编辑器，这是一个三栏布局：

- **左侧**：步骤库，列出所有可用节点——打开网页、点击、输入、等待、提取数据、读写文件
- **中间**：画布区，从左边拖节点过来，连上线，流程就有了
- **右侧**：配置面板，点开节点填参数——URL 是什么、点哪个选择器、提取什么字段

不止是能拖。编辑器还补齐了 **自动保存、自动布局、撤销重做、搜索克隆、批量删除** 这些高频操作。（停顿）*能拖只是入门，让人长期用着顺手才是目标。*

---

## P7 · 亮点②：运行监控实时闭环（约 30 秒）★

**第二，运行监控。**

用户点运行之后，后端创建执行会话、打开浏览器、按步骤顺序执行。关键来了——**这不是黑盒。**

每一步开始、成功、失败、日志输出，全部通过 **WebSocket 实时推**到前端。你能清楚看到：

- 当前卡在第几步
- 已完成的标绿色
- 失败的标红色
- 日志区实时滚动

> 这个闭环把浏览器自动化从 *"跑完看结果"* 变成了 *"跑的过程中全程可观测"*。

---

## P8 · 亮点③：调试工作台（约 30 秒）★

**第三，调试。**

自动化流程跑失败了，光告诉你 *"执行失败"* 没有用——你得知道：**失败在哪一步？当时页面什么状态？变量值是多少？**

TheTower 的调试模式做了三件事：

1. **条件断点**：设定满足某个条件时自动暂停——比如"第三次循环时停下"
2. **时间旅行**：暂停后回看历史步骤的 *变量快照和页面状态*，像倒带一样
3. **远程操控**：在调试预览区直接点击、输入，操控真实浏览器

> 对于复杂流程，这个能力决定了你是 **花 5 分钟** 还是 **花 2 小时** 排查问题。

---

## P9 · 数据与集成能力（约 20 秒，走马观花）

以上是三大亮点。下面快速过一遍其他能力——为后面的演示做铺垫。

节点方面，除了基础的页面操作，还覆盖了 **数据提取、文件读写、Excel 导入导出、剪贴板读取、网络请求监听**。

这意味着一个流程可以从 *导入 Excel* → *逐行录入网页* → *截取接口响应* → *导出处理报告*，**跑通完整数据链路**。

---

## P10 · 产品化能力闭环（约 20 秒）

功能做全了不算完，还得做成一个 **能交付的系统**。

0.1.0 版本补齐了四个产品化模块：**模板市场**（发布和导入复用）、**调度任务**（单次和周期触发）、**认证与工作空间**（多用户隔离）、**协作编辑**（分享和在线成员感知）。

六个模块——编排、执行、调试、数据、平台、协议——**全部闭环**。（停顿）系统不再只是一个技术原型。

---

## P11 · 测试数据仪表盘（约 20 秒）

用数据说话。

项目从 0.0.1 到 0.1.0 **十个版本迭代推进**，累计 **142 项测试用例**，总通过率 **97.2%**。七大模块中五个 100% 通过。

性能指标方面：**50 节点画布流畅拖拽、Chrome/Firefox/Edge 三浏览器兼容、3 流程并发执行**——全部通过验证。

---

## P12 · 核心设计决策（约 15 秒）

三个关键技术选择简单提一下：

- **为什么 ReactFlow 而非自建画布？** 它内置了节点/边模型、拖拽缩放、生态成熟，不需要从零造轮子
- **为什么 REST + WebSocket 而非纯轮询？** 管理接口要清晰，执行状态要实时——两个场景两种协议更合适
- **为什么 SQLite 而非 PostgreSQL？** 毕业设计场景下零配置、零运维、单文件部署

---

## P13 · 总结与展望（约 20 秒）

做个总结。

TheTower 从 **"让浏览器自动化更简单"** 这个出发点，完成了一个具备 **可视化编排、浏览器执行、调试排障、产品化首版能力** 的工作流系统。

不足的方面：协作的自动合并、更细的权限、更完整的自动化测试、更强的存储——这些是后续可以继续推进的方向。

> 以上就是我的汇报，接下来我花三分钟做一个现场演示。

---

## P14 · 致谢（约 5 秒）

*（翻到致谢页，鞠躬）*

感谢各位老师，请多指教。

*（切换到系统界面，开始演示）*

---

## 附：答辩预埋话术

*（以下内容不上 PPT，仅备评委追问时使用）*

---

**Q：为什么不直接用 Selenium？**

> Playwright 相比 Selenium 有三点优势：**自动等待机制**让脚本更稳定，不需要手动写 waitFor；**原生跨浏览器**，Chromium/Firefox/WebKit 同一套 API；**网络拦截和请求监听**能力更强——这对我们的 listenRequest 节点是关键依赖。简单说，*Selenium 能做但更费劲，Playwright 做起来更干净。*

---

**Q：和 UiPath / 来也这类商业 RPA 比，你的差异化在哪？**

> 定位不同。商业 RPA 面向企业级交付，重在全套服务和技术支持，代价是 **授权昂贵且定制受限**。TheTower 面向的是 *"需要一个轻量自动化工具"* 的场景——**开源、本地部署、单文件数据库**。想改代码可以改，想加节点可以加。这种灵活性是商业平台给不了的。

---

**Q：系统最难的技术点是什么？**

> **执行引擎的设计**。因为要在不暴露 Playwright 细节的前提下，把抽象节点稳定映射为具体的浏览器操作。每个节点的执行是异步的、可能失败、需要上下文传递、需要调试支持。这个映射层 *既要简单好扩展，又要保证执行可靠*，花了最多精力。

---

**Q：节点的可扩展性怎么保证？**

> 前端通过 **stepRegistry 注册表**统一管理，新增节点只需定义 type + config schema；后端执行器通过 **类型分发机制**处理，新增节点只需加一个执行分支。目前从 0.0.1 到 0.1.0，节点从五六种扩展到十余种，**没有改过核心架构**。

---

## 附：时间检查点

| 页码 | 累计时间 | 检查点 |
|------|----------|--------|
| P3 结束 | ~1'20" | 核心论点已抛出 |
| P5 结束 | ~2'00" | 架构技术过完，进入重点段 |
| P8 结束 | ~3'30" | 三大亮点讲完，还有 1'30" 余地 |
| P11 结束 | ~4'30" | 能力证据过完，剩 30" 收尾 |
| P14 结束 | ~5'10" | 进入演示 |

**调速指南**：
- 若 P8 结束时已超过 4 分钟 → 跳过 P9（数据节点），P10-P12 各压缩到 15 秒
- 若 P11 结束时还剩 1 分钟以上 → P12 可稍展开，P13 从容收尾
- P5 在任何情况下都不展开，10 秒内带过

---

## 附：演示环节建议流程（3 分钟）

1. **展示编辑器**（40"）：从首页新建模板，拖 3-4 个节点（打开网页 → 点击 → 提取文本），连线，配置参数，保存
2. **普通运行**（50"）：点击运行，展示运行监控面板——步骤逐个变绿、日志实时滚动、运行成功
3. **调试运行**（60"）：在某个节点设断点，调试运行，展示暂停、变量上下文、预览画面，单步执行一次
4. **模板市场**（30"）：切换到首页，打开模板市场，展示已发布的模板列表

建议提前演练 2-3 遍，确保 3 分钟内走完。若超时，优先砍掉模板市场环节。
